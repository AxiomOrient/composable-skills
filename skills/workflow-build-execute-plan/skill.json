{
  "name": "workflow-build-execute-plan",
  "layer": "workflow",
  "layer_badge": "Workflow",
  "default_program": "[stages: preflight>detect>select>implement>critique>sync>verify>loop>audit |\n scope: diff|repo|paths(glob,...) |\n policy: evidence,correctness-first,quality-gates{tests,security},deterministic-output |\n lens: contract-evidence-verifier |\n output: md(contract=v1)]",
  "required_inputs": [
    {
      "name": "MISSION_GOAL",
      "type": "string",
      "required": true,
      "description": "One bounded mission to execute until all task rows are done or a real blocker stops progress."
    },
    {
      "name": "TARGET_SCOPE",
      "type": "path|module|folder|repo",
      "required": true,
      "description": "Scope the loop is allowed to touch."
    },
    {
      "name": "DONE_CONDITION",
      "type": "list",
      "required": true,
      "shape": "{CONDITION, PROOF_REQUIRED}",
      "description": "Explicit completion contract. Typically: all tasks complete + core tests pass."
    },
    {
      "name": "PLAN_ARTIFACTS",
      "type": "list",
      "required": true,
      "shape": "{PATH, ROLE}",
      "description": "Plan and task ledger documents to execute. Required — this workflow does not operate without plan documents. Defaults to plans/IMPLEMENTATION-PLAN.md + plans/TASKS.md. Without this, use control-build-until-done instead."
    },
    {
      "name": "CONSTRAINTS",
      "type": "list",
      "required": false,
      "shape": "{CONSTRAINT}",
      "description": "Safety, rollout, compatibility, or non-goal constraints."
    }
  ],
  "display_name": "Workflow / Build Execute Plan",
  "browse_category": "build",
  "browse_priority": 20,
  "is_category_default": false,
  "codex_surface": "public_entry",
  "starter_inputs": [
    {
      "key": "GOAL",
      "label": "Mission goal",
      "required": true,
      "maps_to": [
        "MISSION_GOAL"
      ],
      "derive_from_raw_request": true
    },
    {
      "key": "SCOPE",
      "label": "Target scope",
      "required": true,
      "maps_to": [
        "TARGET_SCOPE"
      ],
      "derive_from_raw_request": true
    },
    {
      "key": "DONE",
      "label": "Done condition",
      "required": true,
      "maps_to": [
        "DONE_CONDITION"
      ],
      "derive_from_raw_request": true
    },
    {
      "key": "PLAN",
      "label": "Plan/task document paths (required)",
      "required": true,
      "maps_to": [
        "PLAN_ARTIFACTS"
      ],
      "derive_from_raw_request": true
    },
    {
      "key": "CONSTRAINTS",
      "label": "Constraints",
      "required": false,
      "maps_to": [
        "CONSTRAINTS"
      ],
      "derive_from_raw_request": true
    }
  ],
  "expands_to": [
    "$build-write-code",
    "$control-improve-loop",
    "$plan-sync-tasks",
    "$review-final-verify"
  ]
}
