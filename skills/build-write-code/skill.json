{
  "name": "build-write-code",
  "layer": "atomic",
  "layer_badge": "Atomic",
  "default_program": "[stages: preflight>detect>implement>verify>review>audit |\n scope: diff |\n policy: evidence,correctness-first,safety-gates,approval-gates{explicit,no-fallback},deterministic-output |\n lens: hickey-carmack |\n output: md(contract=v1)]",
  "required_inputs": [
    {
      "name": "CHANGE_GOAL",
      "type": "string",
      "required": true,
      "description": "Exact behavior or structure to change."
    },
    {
      "name": "IMPLEMENTATION_MODE",
      "type": "bugfix|feature|refactor|integration|cleanup",
      "required": false,
      "allowed_values": [
        "bugfix",
        "feature",
        "refactor",
        "integration",
        "cleanup"
      ],
      "description": "Implementation mode. Defaults to the narrowest mode implied by the change goal."
    },
    {
      "name": "TARGET_SCOPE",
      "type": "path|module|folder|repo",
      "required": true,
      "description": "Scope of the code changes."
    },
    {
      "name": "TASK_IDS",
      "type": "list",
      "required": false,
      "shape": "{TASK_ID}",
      "description": "Selected task ids from plans/TASKS.md or synthesized adhoc ids when no task ledger exists."
    },
    {
      "name": "TASK_SOURCE",
      "type": "tasks-md|explicit-user|adhoc",
      "required": false,
      "allowed_values": [
        "tasks-md",
        "explicit-user",
        "adhoc"
      ],
      "description": "Where TASK_IDS came from. Use `adhoc` only when no task ledger exists and keep ids stable for this run."
    },
    {
      "name": "VERIFICATION_MAP",
      "type": "list",
      "required": true,
      "shape": "{CHECK, ORDER, PASS_CONDITION}",
      "description": "Narrow-to-broad verification steps."
    },
    {
      "name": "CONSTRAINTS",
      "type": "list",
      "required": false,
      "shape": "{CONSTRAINT}",
      "description": "Safety, rollout, compatibility, or non-goal constraints."
    }
  ],
  "display_name": "Build / Write Code",
  "browse_category": "build",
  "browse_priority": 50,
  "is_category_default": false,
  "codex_surface": "expert",
  "starter_inputs": [
    {
      "key": "GOAL",
      "label": "무엇을 바꿀지",
      "required": true,
      "maps_to": [
        "CHANGE_GOAL"
      ],
      "derive_from_raw_request": true
    },
    {
      "key": "SCOPE",
      "label": "어디를 바꿀지",
      "required": true,
      "maps_to": [
        "TARGET_SCOPE"
      ],
      "derive_from_raw_request": true
    },
    {
      "key": "DONE",
      "label": "검증 기준",
      "required": true,
      "maps_to": [
        "VERIFICATION_MAP"
      ],
      "derive_from_raw_request": true
    },
    {
      "key": "CONSTRAINTS",
      "label": "제약",
      "required": false,
      "maps_to": [
        "CONSTRAINTS"
      ],
      "derive_from_raw_request": true
    }
  ]
}
