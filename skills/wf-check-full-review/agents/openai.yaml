interface:
  display_name: "Full Review Workflow"
  short_description: "Workflow skill that composes neutral structure analysis and bounded review scans for a project, folder, or module. Use when the user wants a review built from explicit subchecks instead of a single broad 'find bugs' instruction."
  default_prompt: "$wf-check-full-review"
policy:
  allow_implicit_invocation: true
