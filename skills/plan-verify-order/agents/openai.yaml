interface:
  display_name: "Verification Map Plan"
  short_description: "Define the narrow-to-broad verification map before changing code. Use when the immediate job is to decide how success will be verified, not to implement the change yet."
  default_prompt: "$plan-verify-order"
policy:
  allow_implicit_invocation: true
