{
  "name": "clarify-boundaries",
  "layer": "atomic",
  "layer_badge": "Atomic",
  "default_program": "[stages: preflight>detect>analyze>plan>handoff>audit | scope: repo|diff|paths(glob,...) | policy: evidence,quality-gates{docs},deterministic-output | lens: minto-pyramid | output: md(contract=v1)]",
  "required_inputs": [
    { "name": "REQUEST", "type": "string", "required": true, "description": "Original user request or work item." },
    { "name": "TARGET_SCOPE", "type": "path|module|repo|artifact", "required": true, "description": "Bounded area under discussion." },
    { "name": "KNOWN_CONSTRAINTS", "type": "list", "required": false, "shape": "{CONSTRAINT}", "description": "Explicit constraints or non-goals already known." }
  ],
  "display_name": "Clarify / Boundaries",
  "browse_category": "clarify",
  "browse_priority": 20,
  "is_category_default": false,
  "codex_surface": "expert",
  "starter_inputs": [
    { "key": "REQUEST", "label": "무엇을 해달라고 했는지", "required": true, "maps_to": ["REQUEST"], "derive_from_raw_request": true },
    { "key": "SCOPE", "label": "어디에 대한 요청인지", "required": true, "maps_to": ["TARGET_SCOPE"], "derive_from_raw_request": true },
    { "key": "CONSTRAINTS", "label": "이미 아는 제약 조건", "required": false, "maps_to": ["KNOWN_CONSTRAINTS"] }
  ]
}
