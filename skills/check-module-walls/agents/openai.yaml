interface:
  display_name: "Boundary Contract Check"
  short_description: "Check whether module, API, or layer boundaries expose unclear contracts, hidden assumptions, or leaking responsibilities. Use when the goal is to verify boundaries rather than perform broad review."
  default_prompt: "$check-module-walls"
policy:
  allow_implicit_invocation: true
