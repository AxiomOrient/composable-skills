interface:
  display_name: "Workflow / Consensus Engine"
  short_description: "Workflow skill that locks a bounded scope contract and then runs the consensus engine to arbitrate one design, debugging, or planning decision across Codex, Claude Code, and Gemini CLI. Use when the public entrypoint should expose multi-model consensus without hiding the boundary-setting step."
  default_prompt: "$workflow-consensus-engine"
policy:
  allow_implicit_invocation: true
