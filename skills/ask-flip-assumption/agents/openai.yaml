interface:
  display_name: "Question Reframe"
  short_description: "Challenge the default framing by listing assumptions, reversing them, and generating a small set of perspective-shifting questions. Use when the question is stuck, too feature-centric, or needs a sharper angle."
  default_prompt: "$ask-flip-assumption"
policy:
  allow_implicit_invocation: true
