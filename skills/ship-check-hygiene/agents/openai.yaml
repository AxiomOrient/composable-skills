interface:
  display_name: "Release Hygiene Check"
  short_description: "Use when release readiness depends on legacy removal, document freshness, and public-surface sync. Do not use for branch mutation or rollout-risk judgement."
  default_prompt: "$ship-check-hygiene"
policy:
  allow_implicit_invocation: true
