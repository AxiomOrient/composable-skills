{
  "name": "plan-dependency-rules",
  "layer": "atomic",
  "layer_badge": "Atomic",
  "default_program": "[stages: preflight>detect>analyze>plan>review>handoff>audit | scope: repo|diff|paths(glob,...) | policy: evidence,quality-gates{compat},deterministic-output | lens: uncle-bob | output: md(contract=v1)]",
  "required_inputs": [
    {
      "name": "TARGET_SCOPE",
      "type": "path|module|layer",
      "required": true,
      "description": "Boundary whose dependency rules will be defined."
    },
    {
      "name": "CURRENT_BOUNDARY_NOTES",
      "type": "list",
      "required": false,
      "shape": "{NOTE, EVIDENCE}",
      "description": "Known dependency problems or leaks."
    }
  ],
  "display_name": "Plan / Dependency Rules",
  "browse_category": "plan",
  "browse_priority": 50,
  "is_category_default": false,
  "codex_surface": "expert",
  "starter_inputs": []
}
