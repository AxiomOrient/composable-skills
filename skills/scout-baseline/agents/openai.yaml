interface:
  display_name: "Scout Baseline"
  short_description: "Capture the exact performance metric, baseline, and budget before optimization. Use when the immediate job is to define and record the baseline, not to optimize yet."
  default_prompt: "$scout-baseline"
policy:
  allow_implicit_invocation: true
