{
  "name": "compose",
  "layer": "utility",
  "layer_badge": "Utility",
  "default_program": "[orchestration-only]",
  "required_inputs": [
    {
      "name": "MACRO_EXPRESSION",
      "type": "string",
      "required": true,
      "shape": "$skill + $workflow-name + @path + [prompt]",
      "description": "Explicit macro text such as $workflow-review-change + @src/auth + $review-final-verify."
    },
    {
      "name": "LENS_OVERRIDE",
      "type": "string",
      "required": false,
      "description": "Optional explicit lens override token."
    },
    {
      "name": "SCOPE_OVERRIDE",
      "type": "string|list",
      "required": false,
      "description": "Optional explicit scope tokens such as @src or scope:paths(src/auth)."
    }
  ],
  "display_name": "Compose",
  "browse_category": "utility",
  "browse_priority": 90,
  "is_category_default": false,
  "codex_surface": "internal_control",
  "starter_inputs": []
}
